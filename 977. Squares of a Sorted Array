class Solution:
    def sortedSquares(self, A: List[int]) -> List[int]:
        output = []
        for i in range(len(A)):
            output.append((A[i]) ** 2)
        output.sort()
        reversed(output)
        return output
